{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd server && npm install && npx prisma generate && cd .. && npx prisma generate --schema=server/prisma/schema.prisma",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?path=$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "api/server-dist/**"
    }
  }
}
