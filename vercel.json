{
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --production=false && cd server && npm install --production=false && npx prisma generate && cd .. && npx prisma generate --schema=server/prisma/schema.prisma",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?path=$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "api/server-dist/**"
    }
  }
}
