{
  "buildCommand": "cd server && npm install && npx prisma generate && npx tsc && cd .. && cp -r server/dist api/server-dist && cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd server && npm install && npx prisma generate && cd .. && npx prisma generate --schema=server/prisma/schema.prisma",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api?path=$1"
    }
  ],
  "functions": {
    "api/index.ts": {
      "includeFiles": "server/dist/**"
    }
  }
}
