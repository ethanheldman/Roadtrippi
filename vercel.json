{
  "buildCommand": "cd server && npm install && npx prisma generate && npx tsc && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd server && npm install && npx prisma generate",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api?path=$1"
    }
  ]
}
